"use client";

/**
 * pusher-js is kept for future real-time features. The app does not subscribe
 * to channels or listen for execution events today.
 */
export { default as Pusher } from "pusher-js";
